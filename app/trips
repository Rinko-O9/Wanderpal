import Link from 'next/link'

const mockTrips = [
  {
    id: '1',
    title: '意大利之旅',
    destination: '佛罗伦萨 · 罗马',
    date: '2026年4月',
    emoji: '🇮🇹',
    places: 8,
  },
  {
    id: '2',
    title: '关西慢游',
    destination: '京都 · 奈良',
    date: '2023年7月',
    emoji: '🇯🇵',
    places: 5,
  },
]

export default function TripsPage() {
  return (
    <main className="min-h-screen bg-stone-50">
      <nav className="flex items-center justify-between px-8 py-6">
        <Link
          href="/"
          className="text-xl font-semibold tracking-tight text-stone-800"
        >
          旅伴{' '}
          <span className="text-stone-400 font-normal text-base">wanderpal</span>
        </Link>
        <button className="bg-teal-600 text-white text-sm px-4 py-2 rounded-full hover:bg-teal-500 transition-colors">
          + 新建行程
        </button>
      </nav>

      <div className="max-w-2xl mx-auto px-8 py-12">
        <h1 className="text-2xl font-semibold text-stone-800 mb-8">我的行程</h1>

        <div className="space-y-4">
          {mockTrips.map((trip) => (
            <Link key={trip.id} href={`/trips/${trip.id}`}>
              <div className="bg-white rounded-2xl p-6 shadow-sm hover:shadow-md transition-shadow cursor-pointer mb-4">
                <div className="flex items-center gap-4">
                  <div className="text-4xl">{trip.emoji}</div>
                  <div className="flex-1">
                    <h2 className="font-medium text-stone-800">{trip.title}</h2>
                    <p className="text-stone-500 text-sm">{trip.destination}</p>
                    <p className="text-stone-400 text-xs mt-1">
                      {trip.date} · {trip.places} 个地点
                    </p>
                  </div>
                  <span className="text-stone-300 text-lg">→</span>
                </div>
              </div>
            </Link>
          ))}
        </div>
      </div>
    </main>
  )
}